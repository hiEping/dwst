import { PageContainer } from '@ant-design/pro-components';

export default () => {
  const Map = (
    <PageContainer title={'安全态势地图'}>
      <div>态势地图页面</div>
    </PageContainer>
  );
  return Map;
};
